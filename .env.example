# Warp API key — generate one at warp://settings/platform
# Only needed if running without an active Warp session.
# The app will use the local `oz` CLI session auth if this is not set.
WARP_API_KEY=wk-1.your-key-here

# Port for the relay UI (default: 4242)
PORT=4242
